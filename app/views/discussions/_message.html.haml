.message
  .title= message.title if message.title
  .body= message.body if message.body && message.parent_id
  .information
    .created_at= message.created_at.to_s(:short)
    .updated_at= message.updated_at.to_s(:short)

